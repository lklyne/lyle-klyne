---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---
<style>
	/* body {
		background-color: rgb(25, 30, 26);
		color: rgba(255, 255, 255, 0.9);
	} */
</style>

<BaseLayout>
    <a href='/blog' class='blog-back-link'>← Back</a>
    <h1 class="blog-post-title">{frontmatter.title}</h1>
    <!-- <p><em>{frontmatter.description}</em></p> -->
    <!-- <p class='blog-date-published'>Published on {frontmatter.pubDate.slice(0,10)}</p> -->
    <!-- <p>Written by {frontmatter.author}</p> -->
    <img class='blog-header-image' src ={frontmatter.image.url} width="100%" alt={frontmatter.image.alt} />
    <slot />
</BaseLayout>